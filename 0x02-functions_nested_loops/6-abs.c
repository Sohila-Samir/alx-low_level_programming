#include "main.h"

/**
 * _abs -  a function that computes the absolute value of an integer.;
 *
 * @n: the number to get the absloute value of.
 *
 * Return: the absloute value of an integar
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	return (n);
}
