#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'programs store an array of charactres representing
 * a string message to print later on using puts function'
 *
 * Return: always 0 success
 */
int main(void)
{
	char msg[] = "\"Programming is like building a multilingual puzzle";

	puts(msg);

	return (0);
}
