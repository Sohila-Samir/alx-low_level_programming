#include <stdio.h>

/**
 * main - Entry point
 *
 * descriptopn: a C program that prints out a message using printf function.
 *
 * Return: always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
