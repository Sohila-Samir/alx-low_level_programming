#include "main.h"

/**
 * main - tests the correct output for the positive_or_negative()
 * function with argument 0.
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}